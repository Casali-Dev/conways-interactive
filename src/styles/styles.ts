import styled from "styled-components";

export const Footer = styled.footer`
  position: absolute;
  bottom: .5rem;
  left: 1rem;

  a {
    text-decoration: none;
    font-weight: 700;
    color: #FFFFFF;

    transition: all 0.2s;

    &:hover {
      color: #6611CD;
    }
  }
`